# Multi-Fidelity Promotion Sweep

{description}

## Steps

1. Run the promotion sweep command (re-evaluates eligible nodes at a higher fidelity tier — may take several minutes per node):
```bash
{command}
```

2. When the command finishes, complete with the command output:
```bash
{complete_command}
```

## Note
Do not interrupt this process. Each promotion re-evaluates a node at a higher fidelity setting, which takes longer than the original low-fidelity run. Let all promotions complete.
